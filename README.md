# react-fetch-with-spinner

To use, first run 

  'yarn add reactjs-simple-spinner'
  
by command prompt at the root folder of the created react application. Then copy these files to the 'src' folder and replace.

